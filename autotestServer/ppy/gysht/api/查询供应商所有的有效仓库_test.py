# NOTE: Generated By HttpRunner v3.1.3
"""
    概要：
        获取供应商的仓库id:storageId
    输入参数：
        无
    输出参数：
        "storageId":该供应商的仓库id
    前置接口：
        用户登陆
"""

from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase仓库查询(HttpRunner):


    config = Config("获取storageId").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "",
            "session":""
        }
    ).export(*["storageId"])

    teststeps = [
        Step(
            RunRequest("获取仓库id")
            .get(
                "/storage/querySupplierAllStorages"
            )
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Accept": "application/json, text/plain, */*",
                    "sessionId": "$session",
                    "User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/84.0.4147.105 Safari/537.36",
                    "Sec-Fetch-Site": "same-site",
                    "Sec-Fetch-Mode": "cors",
                    "Sec-Fetch-Dest": "empty",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,zh-TW;q=0.8,en;q=0.7",
                }
            ).extract()
            .with_jmespath("body.body[0].storageId", "storageId")
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
            ),
    ]


if __name__ == "__main__":
    TestCase仓库查询().test_start()
