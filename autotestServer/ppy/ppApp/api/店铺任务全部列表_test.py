# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        店铺任务全部列表
    输入参数：
        "area": 区域: 1公海 2私海
    输出参数：
        无
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase店铺任务全部列表(HttpRunner):

    config = Config("店铺任务全部列表").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "ppApp",
            "phone": "15757113586",
            "session": "${getSession($business,$phone)}",
            "area": "2"
        })

    teststeps = [
        Step(
            RunRequest("/ppy-pinpin/shopTask/queryTaskList")
            .get("/shopTask/queryTaskList")
            .with_params(**{"area": "$area"})
            .with_headers(
                **{
                    "channel": "app",
                    "Accept-Language": "zh-Hans-CN;q=1",
                    "Accept-Encoding": "gzip, deflate",
                    "Accept": "application/json",
                    "sessionId": "$session",
                    "deviceId": "0380efdbdb0938af0d7a3852519f361a86ff",
                    "serviceName": "NEW-PP",
                    "Connection": "keep-alive",
                }
            )
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
        ),
    ]


if __name__ == "__main__":
    TestCase店铺任务全部列表().test_start()
