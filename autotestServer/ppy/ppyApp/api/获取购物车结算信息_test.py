# NOTE: Generated By HttpRunner v3.1.3
"""
    概要：
        获取购物车结算信息
    输入参数：

    输出参数：

    前置接口：
        验证码登陆
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase获取购物车结算信息(HttpRunner):

    config = Config("获取购物车结算信息").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "",
            "token": "",
        })

    teststeps = [
        Step(
            RunRequest("/ppy-mall/user/cart/settlement")
            .get("/user/cart/settlement")
            .with_headers(
                **{
                    "serviceName": "MALL",
                    "channel": "app",
                    "deviceId": "2cdf701146c959d296b4b455f9623536",
                    "version": "4.4.0",
                    "sessionId": "$token",
                    "Connection": "close",
                    "Accept-Encoding": "gzip",
                    "User-Agent": "okhttp/3.12.0",
                    "Content-Type": "application/json",
                    "Accept-Language": "zh-CN,zh;q=0.8",
                }
            )
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
        ),
    ]


if __name__ == "__main__":
    TestCase获取购物车结算信息().test_start()
