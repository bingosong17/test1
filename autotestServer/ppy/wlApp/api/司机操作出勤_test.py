# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        无
    输入参数：
    "type": 0  出勤状态：0出勤，1请假
    输出参数：
        无
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase司机操作出勤(HttpRunner):

    config = Config("司机操作出勤").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "wlApp",
            "phoneNumber": "15997336112",
            "token": "${getSession($business,$phoneNumber)}",
            "userId": "${getWlAppUserId($phoneNumber)}",
            "date": "${hapentime(48,1,h)}",  # 操作后天出勤状态
            "type": 0
        })

    teststeps = [
        Step(
            RunRequest("/ppy-logis-driver-api/api/attendance/execute")
            .post(
                "/ppy-logis-driver-api/api/attendance/execute"
            )
            .with_headers(
                **{
                    "Content-Length": "164",
                    "Connection": "Keep-Alive",
                    "Accept-Encoding": "gzip",
                    "User-Agent": "okhttp/3.12.1",
                    "Content-Type": "application/json",
                    "Accept-Language": "zh-CN,zh;q=0.8",
                }
            )
            .with_json(
                {
                    "body": {"date": "$date", "type": "$type"},
                    "head": {
                        "channelId": "",
                        "platform": "Android",
                        "token": "$token",
                        "userId": "$userId",
                        "version": "2.13.1",
                    },
                }
            )
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
        ),
    ]


if __name__ == "__main__":
    TestCase司机操作出勤().test_start()
