# NOTE: Generated By HttpRunner v3.1.3
"""
    概要：
        确认照片上传
    输入参数：
        "pickupOrderId": 取货id,
    输出参数：
        "photoList":上传照片的id
    前置接口：
        用户登陆
"""

from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase
from wlApp.api.imgBase64Data import imgBase64Data


class TestCase上传照片(HttpRunner):

    config = Config("testcase description").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "",
            "token": "",
            "userId": "",
            "pickupOrderIdList": "1135199801309941553",
        }
    ).export(*["photoList"])

    teststeps = [
        Step(
            RunRequest("上传照片")
            .post(
                "/ppy-logis-driver-api/upload/batchUploadImg"
            )
            .with_headers(
                **{
                    "Content-Length": "249003",
                    "Connection": "Keep-Alive",
                    "Accept-Encoding": "gzip",
                    "User-Agent": "okhttp/3.14.1",
                    "Content-Type": "application/json",
                    "Accept-Language": "zh-CN,zh;q=0.8",
                }
            )
            .with_json(
                {
                    "body": {
                        "groupName": "PIKCUP_ORDER_RECEIPT",
                        "imgBase64Data":imgBase64Data,
                        "key": "$pickupOrderIdList",
                    },
                    "head": {
                        "channelId": "",
                        "platform": "Android",
                        "token": "$token",
                        "userId": "$userId",
                        "version": "${wlversion()}"},
                }
            ).extract()
            .with_jmespath("body.body[0].imgInfoId", "photoList")
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
        ),

    ]


if __name__ == "__main__":
    TestCase上传照片().test_start()
