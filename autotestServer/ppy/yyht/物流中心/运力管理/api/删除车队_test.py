# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        删除车队
    输入参数：
        fleetId 车队id
    输出参数：
        body    响应体
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase删除车队(HttpRunner):

    config = Config("删除车队").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "yyht",
            "session": "${getSession($business)}",
            "fleetId": ""
        }).export(*["body"])

    teststeps = [
        Step(
            RunRequest("/logis/api/mng/driverFleet/delete")
            .post("/logis/api/mng/driverFleet/delete")
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Content-Length": "33",
                    "Accept": "application/json, text/plain, */*",
                    "sessionId": "$session",
                    "User-Agent": "Mozilla/5.0 (Windows NT 6.1; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/74.0.3729.131 Safari/537.36",
                    "Content-Type": "application/json;charset=UTF-8",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
                }
            )
            .with_json({"fleetId": "$fleetId"}).extract().with_jmespath("body.body","body")
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
            # .assert_equal("body.body", True)
        ),
    ]


if __name__ == "__main__":
    TestCase删除车队().test_start()
