# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        服务主管列表查询
    输入参数：
        name    服务主管名称
    输出参数：
        'name', 服务主管名称
        'serviceId', 服务主管id
        'totalSize   数据条数
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase服务主管列表查询(HttpRunner):

    config = Config("服务主管列表查询").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "yyht",
            "session": "${getSession($business)}",
            "name": ""
        }).export(*['name', 'serviceId','totalSize'])

    teststeps = [
        Step(
            RunRequest("/logis/api/mng/supervisor/list")
            .post("/logis/api/mng/supervisor/list")
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Content-Length": "37",
                    "Accept": "application/json, text/plain, */*",
                    "sessionId": "$session",
                    "User-Agent": "Mozilla/5.0 (Windows NT 6.1; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/74.0.3729.131 Safari/537.36",
                    "Content-Type": "application/json;charset=UTF-8",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
                }
            )
            .with_json({"name": "$name", "pageNum": 1, "pageSize": 15}).extract()
            .with_jmespath("body.body.pageData[0].name", "name")
            .with_jmespath("body.body.pageData[0].serviceId", "serviceId")
            .with_jmespath("body.body.totalSize", "totalSize")
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
        ),
    ]


if __name__ == "__main__":
    TestCase服务主管列表查询().test_start()
