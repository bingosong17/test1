# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        修改服务主管
    输入参数：
            "id": "468",        服务主管id
            "name": "namess",   服务主管名称
            "phone": "13067528941"  服务主管手机号
    输出参数：
        无
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase修改服务主管(HttpRunner):

    config = Config("修改服务主管").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "yyht",
            "session": "${getSession($business)}",
            "id": "468",
            "name": "namess",
            "phone": "13067528941"
        })

    teststeps = [
        Step(
            RunRequest("/logis/api/mng/supervisor/modify")
            .post("/logis/api/mng/supervisor/modify")
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Content-Length": "36",
                    "Accept": "application/json, text/plain, */*",
                    "sessionId": "$session",
                    "User-Agent": "Mozilla/5.0 (Windows NT 6.1; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/74.0.3729.131 Safari/537.36",
                    "Content-Type": "application/x-www-form-urlencoded",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
                }
            )
            .with_data({"id": "$id", "name": "$name", "phone": "$phone"})
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
            .assert_equal("body.body", "操作成功")
        ),
    ]


if __name__ == "__main__":
    TestCase修改服务主管().test_start()
