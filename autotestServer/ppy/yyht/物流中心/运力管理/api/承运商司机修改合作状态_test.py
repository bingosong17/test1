# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        承运商司机修改合作状态
    输入参数：
        driverId
        cooperationStatus   合作状态
    输出参数：
        无
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase承运商司机修改合作状态(HttpRunner):

    config = Config("承运商司机修改合作状态").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "yyht",
            "session": "${getSession($business)}",
            "driverId": "",
            "cooperationStatus": "50"
        })

    teststeps = [
        Step(
            RunRequest("/logis/api/mng/drivers/updateDriverStatus")
            .get(
                "/logis/api/mng/drivers/updateDriverStatus"
            )
            .with_params(**{"driverId": "$driverId", "cooperationStatus": "$cooperationStatus"})
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Accept": "application/json, text/plain, */*",
                    "sessionId": "$session",
                    "User-Agent": "Mozilla/5.0 (Windows NT 6.1; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/74.0.3729.131 Safari/537.36",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
                }
            )
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
            .assert_equal("body.body", "修改成功")
        ),
    ]


if __name__ == "__main__":
    TestCase承运商司机修改合作状态().test_start()
