# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        运力统计
    输入参数：
        date    日期
    输出参数：
        checkVehTypeName 车辆类型
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase运力统计(HttpRunner):

    config = Config("运力统计").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "yyht",
            "session": "${getSession($business)}",
            "date": "${hapentime(0,1)}",
        }).export(*['checkVehTypeName'])

    teststeps = [
        Step(
            RunRequest("/logis/api/mng/attendance/vehicle")
            .post("/logis/api/mng/attendance/vehicle")
            .with_params(**{"date": "$date"})
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Content-Length": "2",
                    "Accept": "application/json, text/plain, */*",
                    "sessionId": "$session",
                    "User-Agent": "Mozilla/5.0 (Windows NT 6.1; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/74.0.3729.131 Safari/537.36",
                    "Content-Type": "application/json;charset=UTF-8",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8",
                }
            )
            .with_json({}).extract().with_jmespath("body.body.stats","checkVehTypeName")
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
        ),
    ]


if __name__ == "__main__":
    TestCase运力统计().test_start()
