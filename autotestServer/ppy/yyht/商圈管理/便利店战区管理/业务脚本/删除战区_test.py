# NOTE: Generated By HttpRunner v3.1.3
"""
    概要：
        按照战区名称删除战区
    输入参数：
        "regionName": 战区名称
        "disable": 战区是否可用，0-可用，1-不可用
    输出参数：
        无
    前置接口：
        用户登陆
"""

from httprunner import HttpRunner, Config, Step, RunTestCase
from yyht.商圈管理.便利店战区管理.api.列表展示_test import TestCase列表展示 as 查找战队
from yyht.商圈管理.便利店战区管理.api.删除战区_test import TestCase删除战区 as 删除战区


class TestCase删除战区(HttpRunner):

    config = Config("testcase description").verify(False).base_url("${testEnv($business)}").variables(
        **{
            "business": "yyht",
            "session": "${getSession($business)}",
            "regionName": "aaaa",
            "disable": 0,
        })

    teststeps = [
        Step(
            RunTestCase("查找战队").call(查找战队).export(*["regionId"])
        ),
        Step(
            RunTestCase("删除战区").call(删除战区)
        ),
    ]


if __name__ == "__main__":
    TestCase删除战区().test_start()
