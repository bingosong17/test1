# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        查看yyht管理用户是否存在
    输入参数：
        "mobile": 用户手机号
    输出参数：
        无
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase查看用户是否存在(HttpRunner):

    config = Config("查看用户是否存在").verify(False).base_url("${testEnv($business,newweb)}").variables(
        **{
            "business": "yyht",
            "session": "",
            "mobile": "13362140179"
        })

    teststeps = [
        Step(
            RunRequest("/API/ppy-auth/api/login/checkIsExternalUser")
            .post(
                "/ppy-auth/api/login/checkIsExternalUser"
            )
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Content-Length": "24",
                    "sessionId": "$session",
                    "systemCode": "yypp",
                    "Content-Type": "application/json",
                    "Accept": "*/*",
                    "Sec-Fetch-Site": "same-origin",
                    "Sec-Fetch-Mode": "cors",
                    "Sec-Fetch-Dest": "empty",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,zh-TW;q=0.8,en;q=0.7",
                }
            )
            .with_json({"mobile": "$mobile"})
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
            .assert_equal("body.body", 0)
        ),
    ]


if __name__ == "__main__":
    TestCase查看用户是否存在().test_start()
