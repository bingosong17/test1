# NOTE: Generated By HttpRunner v3.1.4
"""
    概要：
        发送钉钉验证码
    输入参数：
        "mobile": 用户手机号
    输出参数：
        无
    前置接口：
        无
"""


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCase发送钉钉验证码(HttpRunner):

    config = Config("发送钉钉验证码").verify(False).base_url("${testEnv($business,newweb)}").variables(
        **{
            "business": "yyht",
            "session": "",
            "mobile": "13362140179"
        })

    teststeps = [
        Step(
            RunRequest("/API/ppy-auth/api/dingtalk/messageNotice")
            .post(
                "/ppy-auth/api/dingtalk/messageNotice"
            )
            .with_headers(
                **{
                    "Connection": "keep-alive",
                    "Content-Length": "24",
                    "sessionId": "$session",
                    "systemCode": "yypp",
                    "Content-Type": "application/json",
                    "Accept": "*/*",
                    "Sec-Fetch-Site": "same-origin",
                    "Sec-Fetch-Mode": "cors",
                    "Sec-Fetch-Dest": "empty",
                    "Accept-Encoding": "gzip, deflate, br",
                    "Accept-Language": "zh-CN,zh;q=0.9,zh-TW;q=0.8,en;q=0.7",
                }
            )
            .with_json({"mobile": "$mobile"})
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.statusCode", 2000)
            .assert_equal("body.body", True)
        ),
    ]


if __name__ == "__main__":
    TestCase发送钉钉验证码().test_start()
